package shimmer.enums;

/**
 * This enum names a metric to represent in Simulation.
 * 
 * @author Filip Daca
 */
public enum Metric {

	CLASS_COUNT,
	ABSTRACTNESS,
	INSTABILITY,
	DISTANCE_FROM_MAIN_SEQUENCE;
}
